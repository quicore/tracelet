# tracelet
A lightweight, dependency-free execution context for Node.js that bundles lifecycle tracking, distributed-trace propagation, business metrics, and I/O performance stats into a single self-instrumenting object — then serializes the whole thing to a structured snapshot or a flat log line.
